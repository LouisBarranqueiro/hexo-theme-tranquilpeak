<% if ((typeof tags !== 'undefined') && (tags.length > 0) && (typeof style !== 'undefined')) { %>
    <% tags.limit(15).each(function(item) { %>
        <a class="tag <%= 'tag--' + style %>" href="<%- url_for(item.path) %>"><%= item.name %></a>
    <% }); %>
<% } %>