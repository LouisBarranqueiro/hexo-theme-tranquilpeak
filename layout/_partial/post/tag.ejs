<% if ((typeof tags !== 'undefined') && (tags.length > 0)) { %>
    <% tags.limit(15).each(function(item){ %>
        <a class="tag tag--primary" href="<%- url_for(item.path) %>"><%= item.name %></a>
    <% }); %>
<% } %>