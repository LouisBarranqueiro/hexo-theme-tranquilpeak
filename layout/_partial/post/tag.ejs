<% if (typeof tags !== 'undefined' && tags.length > 0) { %>
    <% tags.sort('length').limit(15).each(function(item){ %>
        <a class="tag" href="<%- config.root %><%- item.path %>"><%= item.name %></a>
    <% }); %>
<% } %>