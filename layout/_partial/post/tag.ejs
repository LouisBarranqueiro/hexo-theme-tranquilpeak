<% if ((tags) && (tags.length > 0)) { %>
    <%- list_tags(tags, {
        show_count: 0,
        style: 'none',
        class: 'tag tag--primary tag--small t',
        separator: ' '
    }) %>
<% } %>